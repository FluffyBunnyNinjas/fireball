apply plugin: "java"
apply plugin: "eclipse"

ext.awsSDKVersion = "1.6.4"

repositories {
    // Central maven repository
    mavenCentral()
}

dependencies {
            // AWS SDK, we need the interfaces and stuff
    compile "com.amazonaws:aws-java-sdk:${awsSDKVersion}",
            files("libs/sqlite4java.jar", "libs/DynamoDBLocal-repack.jar")
            // JUnit 4
    testCompile "junit:junit:4.11"
}

jar {
    FileTree zip = zipTree("libs/DynamoDBLocal-repack.jar")
    from zip
}
